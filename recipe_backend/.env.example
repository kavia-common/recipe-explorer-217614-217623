# Backend environment configuration example

# Database connection string (defaults to SQLite ./app.db if not set)
# Examples:
#   SQLite: sqlite:///./app.db
#   Postgres: postgresql+psycopg://user:password@localhost:5432/recipes
#   MySQL: mysql+pymysql://user:password@localhost:3306/recipes
DATABASE_URL=sqlite:///./app.db

# CORS configuration: comma-separated list of allowed origins
# e.g. http://localhost:3000,https://your-domain.com
CORS_ALLOWED_ORIGINS=http://localhost:3000

# JWT configuration for auth
JWT_SECRET=change-this-in-prod
JWT_ALGORITHM=HS256
ACCESS_TOKEN_EXPIRE_MINUTES=60

# Spoonacular API
SPOONACULAR_API_KEY=your-spoonacular-api-key
