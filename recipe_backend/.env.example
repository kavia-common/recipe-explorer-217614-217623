# Backend environment configuration

# Database connection string (defaults to SQLite file if not provided)
# DATABASE_URL=sqlite:///./app.db

# JWT configuration for auth
# Change these values in your real .env (do not commit secrets)
JWT_SECRET=changeme-super-secret
JWT_ALGORITHM=HS256
ACCESS_TOKEN_EXPIRE_MINUTES=60

# Spoonacular API key (required for /recipes endpoints)
SPOONACULAR_API_KEY=your_spoonacular_api_key_here

# CORS allowed origins (optional; comma-separated)
# CORS_ALLOWED_ORIGINS=http://localhost:3000
